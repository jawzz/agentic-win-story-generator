services:
  - type: web
    name: agentic-win-story-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 45
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    pla